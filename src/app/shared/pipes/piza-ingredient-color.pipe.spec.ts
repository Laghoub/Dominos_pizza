import { PizaIngredientColorPipe } from './piza-ingredient-color.pipe';

describe('PizaIngredientColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PizaIngredientColorPipe();
    expect(pipe).toBeTruthy();
  });
});
